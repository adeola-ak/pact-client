import React, { useState } from "react";
import { GlobalCtx } from "../App";

const Home = (props) => {
	return (
		<div>
			<h1>Welcome</h1>
			<p>please sign up or login to view user dashboard</p>
		</div>
	);
};
export default Home;
